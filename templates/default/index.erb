<% @title = "Home" %>

<% if @posts.empty? %>
	<p>There are no currently no posts!</p>
<% else %>

	<% for post in @posts %>
	<div id="<%=post.id%>">
		<h3><%=post.title%></h3>
		<h4>Posted by <%=post.user.name%></h4>
		<p><%=post.body%></p>
	</div>
	<% end %>
<% end %>
		